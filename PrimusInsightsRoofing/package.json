{
  "name": "primus-home-pro",
  "version": "0.1.0",
  "description": "PrimusHomePro — hybrid roofing + solar lead-capture & home-services platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "cd frontend && next dev",
    "build:frontend": "cd frontend && next build",
    "start:frontend": "cd frontend && next start",
    "dev:backend": "cd backend && ts-node-dev src/index.ts",
    "build:backend": "cd backend && tsc",
    "start:backend": "cd backend && node dist/index.js",
    "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:backend\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
